# P3-d — Rapports HSE (CSV + PDF) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exporter le journal (CSV) et une synthèse de conformité (PDF ReportLab) via deux endpoints de téléchargement, et activer le bouton « Exporter » du dashboard.

**Architecture:** Générateurs **purs** (`events_csv` → `str`, `summary_pdf` → `bytes`) séparés des endpoints ; endpoints lisant `app.state.journal` et renvoyant une réponse `attachment` ; frontend construisant les URLs de téléchargement avec les filtres courants.

**Tech Stack:** Python 3.13, FastAPI, `csv` (stdlib), **ReportLab** (nouvelle dépendance, pur Python), Next.js 16, Vitest.

## Global Constraints

- **CSV = stdlib `csv`** ; **PDF = ReportLab** (ajouté à `requirements.txt` **et**
  `requirements-dev.txt` — la CI backend installe le dev, qui est autonome).
- **Génération pure et testable** (dict/list → `str`/`bytes`), hors endpoints.
- **PDF = synthèse + tableaux** (en-tête site/période + taux global/zone + tableau
  infractions) ; **pas de vignette** (V2).
- Réutilise les filtres `Journal` (`zone`, `ppe`, `since`, `until`), `limit=1000` pour les
  exports.
- Frontend : liens `<a href download>` vers `${API}/reports/…` (téléchargement natif).
- Interpréteur test backend : **`py -3`** ; frontend : `npm run test` / `npm run build`.
- Suites existantes vertes (backend **97**, frontend **44**). Commits conventionnels, anglais,
  **sans `Co-Authored-By`**.

---

### Task 1: Générateur CSV (`events_csv`)

**Files:**
- Create: `backend/app/reports/__init__.py` (vide), `backend/app/reports/csv_report.py`
- Test: `backend/tests/test_csv_report.py`

**Interfaces:**
- Produces: `events_csv(events: list[dict]) -> str` (entête FR + une ligne par event).

- [ ] **Step 1: Écrire le test qui échoue** — `backend/tests/test_csv_report.py`
```python
from __future__ import annotations

from app.reports.csv_report import events_csv


def test_events_csv_header_and_rows():
    out = events_csv([
        {"ts": "2026-08-04T12:00", "zone": "Fonderie", "track_id": 37,
         "missing": ["helmet", "shoes"], "camera": "cam-1", "snapshot": "a.jpg"},
        {"ts": "2026-08-04T12:01", "zone": None, "track_id": 8,
         "missing": ["mask"], "camera": "cam-1", "snapshot": None},
    ])
    lines = out.strip().splitlines()
    assert lines[0] == "Heure,Zone,Personne,EPI manquants,Caméra,Preuve"
    assert "#37" in lines[1] and '"helmet, shoes"' in lines[1] and "a.jpg" in lines[1]
    assert "#8" in lines[2] and "mask" in lines[2]


def test_events_csv_empty_has_only_header():
    assert events_csv([]).strip() == "Heure,Zone,Personne,EPI manquants,Caméra,Preuve"
```

- [ ] **Step 2: Lancer le test** — `cd backend && py -3 -m pytest tests/test_csv_report.py -q` → FAIL.

- [ ] **Step 3: Écrire l'implémentation**

`backend/app/reports/__init__.py` : vide.

`backend/app/reports/csv_report.py` :
```python
from __future__ import annotations

import csv
import io

_HEADER = ["Heure", "Zone", "Personne", "EPI manquants", "Caméra", "Preuve"]


def events_csv(events: list[dict]) -> str:
    buf = io.StringIO()
    writer = csv.writer(buf)
    writer.writerow(_HEADER)
    for e in events:
        writer.writerow([
            e.get("ts", ""),
            e.get("zone") or "",
            f"#{e['track_id']}",
            ", ".join(e.get("missing", [])),
            e.get("camera", ""),
            e.get("snapshot") or "",
        ])
    return buf.getvalue()
```

- [ ] **Step 4: Lancer le test** — PASS (2).

- [ ] **Step 5: Commit**
```bash
git add backend/app/reports/__init__.py backend/app/reports/csv_report.py backend/tests/test_csv_report.py
git commit -m "feat(backend): CSV report generator for the violation journal"
```

---

### Task 2: Générateur PDF (`summary_pdf`) + dépendance ReportLab

**Files:**
- Create: `backend/app/reports/pdf_report.py`
- Modify: `backend/requirements.txt`, `backend/requirements-dev.txt`
- Test: `backend/tests/test_pdf_report.py`

**Interfaces:**
- Produces: `summary_pdf(stats: dict, events: list[dict], meta: dict) -> bytes`.

- [ ] **Step 1: Ajouter la dépendance et l'installer**

Ajouter `reportlab` en fin de `backend/requirements.txt` et de `backend/requirements-dev.txt`.
Puis : `cd backend && py -3 -m pip install reportlab`.

- [ ] **Step 2: Écrire le test qui échoue** — `backend/tests/test_pdf_report.py`
```python
from __future__ import annotations

from app.reports.pdf_report import summary_pdf


def test_summary_pdf_returns_pdf_bytes():
    stats = {"global": {"rate": 0.9},
             "by_zone": [{"zone": "Fonderie", "rate": 0.9}],
             "over_time": [],
             "violations": {"total": 2, "by_zone": {"Fonderie": 2}}}
    events = [{"ts": "2026-08-04T12:00", "zone": "Fonderie", "track_id": 37,
               "missing": ["helmet"]}]
    pdf = summary_pdf(stats, events,
                      {"site": "Meknès-Nord", "since": None, "until": None,
                       "generated_at": "2026-08-04"})
    assert pdf[:4] == b"%PDF"
    assert len(pdf) > 500


def test_summary_pdf_handles_empty():
    pdf = summary_pdf({"global": {}, "by_zone": [], "over_time": [],
                       "violations": {"total": 0, "by_zone": {}}}, [], {})
    assert pdf[:4] == b"%PDF"
```

- [ ] **Step 3: Lancer le test** — `cd backend && py -3 -m pytest tests/test_pdf_report.py -q` → FAIL.

- [ ] **Step 4: Écrire l'implémentation** — `backend/app/reports/pdf_report.py`
```python
from __future__ import annotations

import io

from reportlab.lib import colors
from reportlab.lib.pagesizes import A4
from reportlab.lib.styles import getSampleStyleSheet
from reportlab.platypus import Paragraph, SimpleDocTemplate, Spacer, Table, TableStyle


def _pct(rate) -> str:
    return "—" if rate is None else f"{round(rate * 100)}%"


def _table(rows: list[list[str]]) -> Table:
    t = Table(rows)
    t.setStyle(TableStyle([
        ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#0B0D12")),
        ("TEXTCOLOR", (0, 0), (-1, 0), colors.white),
        ("GRID", (0, 0), (-1, -1), 0.5, colors.grey),
        ("FONTSIZE", (0, 0), (-1, -1), 8),
    ]))
    return t


def summary_pdf(stats: dict, events: list[dict], meta: dict) -> bytes:
    buf = io.BytesIO()
    doc = SimpleDocTemplate(buf, pagesize=A4, title="Rapport de conformité EPI — Argus")
    styles = getSampleStyleSheet()
    story: list = [
        Paragraph("Rapport de conformité EPI — Argus", styles["Title"]),
        Paragraph(f"Site : {meta.get('site', 'Argus')}", styles["Normal"]),
        Paragraph(
            f"Période : {meta.get('since') or 'début'} → {meta.get('until') or 'maintenant'}",
            styles["Normal"]),
        Paragraph(f"Généré le {meta.get('generated_at', '')}", styles["Normal"]),
        Spacer(1, 12),
    ]

    g = stats.get("global", {})
    v = stats.get("violations", {})
    story += [
        Paragraph("Synthèse", styles["Heading2"]),
        _table([["Indicateur", "Valeur"],
                ["Conformité globale", _pct(g.get("rate"))],
                ["Infractions", str(v.get("total", 0))]]),
        Spacer(1, 12),
    ]

    vbz = v.get("by_zone", {})
    zrows = [["Zone", "Taux", "Infractions"]]
    for z in stats.get("by_zone", []):
        zrows.append([z["zone"], _pct(z.get("rate")), str(vbz.get(z["zone"], 0))])
    story += [Paragraph("Conformité par zone", styles["Heading2"]), _table(zrows),
              Spacer(1, 12)]

    erows = [["Heure", "Zone", "Personne", "EPI manquants"]]
    for e in events:
        erows.append([e.get("ts", ""), e.get("zone") or "—",
                      f"#{e['track_id']}", ", ".join(e.get("missing", []))])
    story += [Paragraph("Journal des infractions", styles["Heading2"]), _table(erows)]

    doc.build(story)
    return buf.getvalue()
```

- [ ] **Step 5: Lancer le test** — PASS (2).

- [ ] **Step 6: Commit**
```bash
git add backend/app/reports/pdf_report.py backend/requirements.txt backend/requirements-dev.txt backend/tests/test_pdf_report.py
git commit -m "feat(backend): PDF summary report (ReportLab)"
```

---

### Task 3: Endpoints `/reports/*`

**Files:**
- Modify: `backend/app/api/app.py`
- Test: `backend/tests/test_reports_api.py`

**Interfaces:**
- Consumes: `events_csv` (T1), `summary_pdf` (T2), `Journal.events`/`stats`.
- Produces: `GET /reports/events.csv` (text/csv, attachment) ; `GET /reports/summary.pdf`
  (application/pdf, attachment).

- [ ] **Step 1: Écrire le test qui échoue** — `backend/tests/test_reports_api.py`
```python
from __future__ import annotations

from datetime import datetime, timezone

from fastapi.testclient import TestClient

from app.api.app import create_app
from app.domain.types import ViolationEvent
from app.persistence.journal import Journal


def _client(journal):
    app = create_app()
    app.state.detector = object()
    app.state.decode = lambda b64: b64
    app.state.journal = journal
    return TestClient(app)


def _seed(journal):
    journal.record_event(
        ViolationEvent(track_id=37, zone="Fonderie", missing=frozenset({"helmet"}),
                       timestamp=0.0, camera="cam-1"),
        datetime(2026, 8, 4, 12, 0, tzinfo=timezone.utc))


def test_events_csv_download():
    journal = Journal(":memory:")
    _seed(journal)
    r = _client(journal).get("/reports/events.csv")
    assert r.status_code == 200
    assert r.headers["content-type"].startswith("text/csv")
    assert "attachment" in r.headers["content-disposition"]
    assert "#37" in r.text and "helmet" in r.text


def test_summary_pdf_download():
    journal = Journal(":memory:")
    _seed(journal)
    r = _client(journal).get("/reports/summary.pdf")
    assert r.status_code == 200
    assert r.headers["content-type"] == "application/pdf"
    assert "attachment" in r.headers["content-disposition"]
    assert r.content[:4] == b"%PDF"
```

- [ ] **Step 2: Lancer le test** — `cd backend && py -3 -m pytest tests/test_reports_api.py -q` → FAIL (404).

- [ ] **Step 3: Écrire l'implémentation** — éditer `backend/app/api/app.py`

(a) Ajouter `Response` à l'import des réponses :
```python
from fastapi.responses import FileResponse, Response
```

(b) Ajouter les deux endpoints après `get_event_snapshot` (avant le handler websocket) :
```python
    @app.get("/reports/events.csv")
    def report_events_csv(
        zone: str | None = None,
        ppe: str | None = None,
        since: str | None = None,
        until: str | None = None,
    ) -> Response:
        from app.reports.csv_report import events_csv

        events = app.state.journal.events(
            zone=zone, ppe=ppe, since=since, until=until, limit=1000)
        return Response(
            content=events_csv(events), media_type="text/csv",
            headers={"Content-Disposition": 'attachment; filename="argus-events.csv"'})

    @app.get("/reports/summary.pdf")
    def report_summary_pdf(
        since: str | None = None,
        until: str | None = None,
        zone: str | None = None,
    ) -> Response:
        from app.reports.pdf_report import summary_pdf

        stats = app.state.journal.stats(since=since, until=until, zone=zone)
        events = app.state.journal.events(zone=zone, since=since, until=until, limit=1000)
        meta = {
            "site": os.environ.get("ARGUS_SITE", "Meknès-Nord"),
            "since": since, "until": until,
            "generated_at": datetime.now(timezone.utc).strftime("%Y-%m-%d %H:%M UTC"),
        }
        return Response(
            content=summary_pdf(stats, events, meta), media_type="application/pdf",
            headers={"Content-Disposition": 'attachment; filename="argus-rapport.pdf"'})
```

- [ ] **Step 4: Lancer le test** — `cd backend && py -3 -m pytest tests/test_reports_api.py -q` → PASS (2).

- [ ] **Step 5: Commit**
```bash
git add backend/app/api/app.py backend/tests/test_reports_api.py
git commit -m "feat(backend): GET /reports/events.csv and /reports/summary.pdf"
```

---

### Task 4: Frontend — activer l'export

**Files:**
- Create: `frontend/lib/reportsApi.ts`, `frontend/lib/reportsApi.test.ts`
- Modify: `frontend/components/dashboard/Dashboard.tsx`, `frontend/components/dashboard/Dashboard.test.tsx`

**Interfaces:**
- Consumes: `API`/`qs` (`lib/http`), `sinceFor` (dans `Dashboard`).
- Produces: `reportUrl(kind: "csv" | "pdf", params?) -> string` ; boutons CSV/PDF actifs.

- [ ] **Step 1: Écrire le test qui échoue** — `frontend/lib/reportsApi.test.ts`
```ts
import { reportUrl } from "./reportsApi";

test("reportUrl construit les URLs CSV/PDF avec filtres", () => {
  expect(reportUrl("csv", { zone: "Fonderie", ppe: "helmet" })).toContain("/reports/events.csv?");
  expect(reportUrl("csv", { zone: "Fonderie" })).toContain("zone=Fonderie");
  expect(reportUrl("pdf", { since: "2026-08-04T00:00" })).toContain("/reports/summary.pdf?");
});
```

- [ ] **Step 2: Lancer le test** — `cd frontend && npx vitest run lib/reportsApi.test.ts` → FAIL.

- [ ] **Step 3: Écrire l'implémentation**

`frontend/lib/reportsApi.ts` :
```ts
import { API, qs } from "./http";

export function reportUrl(
  kind: "csv" | "pdf",
  params: { zone?: string; ppe?: string; since?: string } = {},
): string {
  const path = kind === "csv" ? "/reports/events.csv" : "/reports/summary.pdf";
  return `${API}${path}${qs(params)}`;
}
```

Modifier `frontend/components/dashboard/Dashboard.tsx` :
- ajouter l'import : `import { reportUrl } from "@/lib/reportsApi";`
- dans le corps du composant (avant le `return`), calculer les paramètres d'export :
```tsx
  const exportParams = {
    zone: filters.zone || undefined,
    ppe: filters.ppe || undefined,
    since: sinceFor(filters.range),
  };
```
- remplacer le bloc du bouton inerte :
```tsx
          <button disabled title="Rapports — P3-d"
                  className="cursor-not-allowed rounded-lg border border-line2 px-3 py-1.5 text-[12px] font-bold text-ink3 opacity-50">
            Exporter
          </button>
```
par :
```tsx
          <div className="flex gap-1.5">
            <a href={reportUrl("csv", exportParams)} download
               className="rounded-lg border border-line2 px-3 py-1.5 text-[12px] font-bold text-ink hover:bg-s2">
              CSV
            </a>
            <a href={reportUrl("pdf", exportParams)} download
               className="rounded-lg border border-line2 px-3 py-1.5 text-[12px] font-bold text-ink hover:bg-s2">
              PDF
            </a>
          </div>
```

- [ ] **Step 4: Adapter `Dashboard.test.tsx`** — ajouter, à la fin du test existant, l'assertion des liens d'export :
```tsx
  expect(screen.getByText("CSV")).toHaveAttribute("href", expect.stringContaining("/reports/events.csv"));
  expect(screen.getByText("PDF")).toHaveAttribute("href", expect.stringContaining("/reports/summary.pdf"));
```

- [ ] **Step 5: Lancer les tests + build**

Run: `cd frontend && npm run test && npm run build`
Expected: tous verts (nouveau `reportsApi` + `Dashboard` avec liens d'export ; suite existante inchangée) ; build OK.

- [ ] **Step 6: Commit**
```bash
git add frontend/lib/reportsApi.ts frontend/lib/reportsApi.test.ts frontend/components/dashboard/Dashboard.tsx frontend/components/dashboard/Dashboard.test.tsx
git commit -m "feat(frontend): activate dashboard export (CSV/PDF download links)"
```

---

## Self-Review

**1. Couverture spec (P3-d) :** `events_csv` ✅ (T1) ; `summary_pdf` ReportLab + dépendance
(2 requirements) ✅ (T2) ; endpoints `/reports/events.csv` + `/reports/summary.pdf` en
téléchargement ✅ (T3) ; bouton export activé (CSV/PDF avec filtres) + `reportUrl` ✅ (T4).
PDF = synthèse + tableaux, pas de vignette ✅. Suites existantes préservées (T3 endpoints,
T4 build).

**2. Placeholders :** aucun — code complet (CSV, PDF, endpoints, frontend, tests). L'édition
`app.py` (T3) est ancrée (import réponses + endpoints après `get_event_snapshot`).

**3. Cohérence des types :** dict d'event de `Journal.events` (clés `ts, zone, track_id,
missing, camera, snapshot`) consommé par `events_csv` (T1) et `summary_pdf` (T2) et les
endpoints (T3). `reportUrl` (T4) construit les mêmes chemins que les endpoints (T3) ;
`exportParams` réutilise `sinceFor` déjà présent dans `Dashboard`. `Response` importé une
seule fois à côté de `FileResponse`.
